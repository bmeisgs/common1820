let array = [5, 1, 4, 7, 9, -12]

let length = array.length;

let values = []

for(0; length-1 < array.length; length++){
   values.push = array[length]-array[length+1]
}

let display = values[Number.MIN_VALUE]

console.log(display)