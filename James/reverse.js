let sampleArray = [1, 2, 3, 4, 5]

function reverseArray(sampleArray) {
    let length = sampleArray.length
    let newSampleArray = [] 
    for(; length > 0 ; length--) {
        push sampleArray.length[length]
    }
console.log(newSampleArray)
}

reverseArray(sampleArray)

